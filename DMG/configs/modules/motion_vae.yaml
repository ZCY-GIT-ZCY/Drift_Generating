# MLD VAE 配置（复用）
motion_vae:
  target: dmg.models.architectures.mld_vae.MldVae
  params:
    nfeats: 263
    latent_dim: [1, 256]
    ff_size: 1024
    num_layers: 9
    num_heads: 4
    dropout: 0.1
    activation: gelu
    arch: all_encoder
    mlp_dist: False